[package]
name = "void-ffi"
version = "1.0.2"
edition = "2021"

[lib]
# cdylib  — the shared library consumed by other languages
# rlib    — needed for Rust integration tests in tests/
crate-type = ["cdylib", "rlib"]

[dependencies]
void = { path = "../void" }
